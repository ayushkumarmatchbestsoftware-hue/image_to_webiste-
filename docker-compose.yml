services:
  web:
    build: .
    ports:
      - "5077:5077"
    env_file:
      - .env
    command: ["flask", "run", "--host=0.0.0.0", "--port=5077"]
    volumes:
      - .:/app
    networks:
      - cloudflare_net

  worker:
    build: .
    env_file:
      - .env
    command: ["python", "worker.py"]
    volumes:
      - .:/app
    networks:
      - cloudflare_net

networks:
  cloudflare_net:
    external: true
